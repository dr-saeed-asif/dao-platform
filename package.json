{
  "name": "dao-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready DAO platform for CyberChain",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build --workspace=api && npm run build --workspace=@dao-platform/web",
    "build:packages": "npm run build --workspace=@dao-platform/domain && npm run build --workspace=@dao-platform/application && npm run build --workspace=@dao-platform/blockchain-cyberchain && npm run build --workspace=@dao-platform/database && npm run build --workspace=@dao-platform/contracts",
    "test": "npm run build && npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "env:migrate-sdk": "node scripts/migrate-sdk-env.mjs",
    "dev:api": "npm run start:dev --workspace=api",
    "dev:web": "npm run dev --workspace=@dao-platform/web",
    "db:migrate": "npm run db:migrate --workspace=@dao-platform/database",
    "db:inspect": "npm run db:inspect --workspace=@dao-platform/database"
  }
}
